import {
  avgPrevTimeDistance, avgTimeDistance, ensureDate, getTimeSteps, IStatConfig, iterateEachInFind, makeStat, makeStatBackend, mean, StatRegistry, StatType, sum, sumPrevTimeDistance, sumTimeDistance, timeStep, timeStepSum
} from "@xpfw/dm"

export {
  mean, sum, timeStepSum, sumTimeDistance, sumPrevTimeDistance, avgTimeDistance, avgPrevTimeDistance, timeStep, getTimeSteps, ensureDate, iterateEachInFind, makeStat, makeStatBackend, IStatConfig, StatType, StatRegistry
}
